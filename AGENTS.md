# Rainbow Heart Studio collaborator entry

Before planning or changing this application, read in order:

1. `../rainbowheart-os/START_HERE.md`
2. `DESIGN_PROFILE.json`
3. `DESIGN_HANDOFF.md`
4. `../Welcome.md`
5. `../Projects/Brother Jon's Songwriter Session.md`
6. The actual source and working tree

Rainbow Heart OS is the shared, vendor-neutral source of truth for design workflow and identities. This adapter does not grant production mutation, deployment, deletion, migration, account creation, or permission changes.
# Shared Memory preflight

If Rainbow Heart Team Shared Memory is available, retrieve current `proj-002` context before substantial work using a narrow task-specific query. Cite and then reread each returned canonical source before editing. Retrieval never overrides this file, `DESIGN_PROFILE.json`, `DESIGN_HANDOFF.md`, or the user's current request. Follow `../rainbowheart-os/MEMORY_TAXONOMY.md`; do not index secrets or raw chain-of-thought.

